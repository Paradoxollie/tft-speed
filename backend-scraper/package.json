{
  "name": "backend-scraper",
  "version": "1.0.0",
  "description": "A Node.js TypeScript backend for web scraping with Puppeteer",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "typescript",
    "nodejs",
    "puppeteer",
    "scraper",
    "backend"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^23.10.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
